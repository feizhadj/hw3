<h1>New Place</h1>

<%= form_for @place, url: "/place" do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Create' %>
<% end %>

<%= link_to 'Back', "/place" %>