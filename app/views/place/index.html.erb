<h1>Places I've Been</h1>

<%= link_to 'Sort by Name', "/place" %>

<% @places.each do |place| %>
  <h2><%= link_to place.name, "/palce" %></h2>
  <p><%= place.description %></p>
<% end %>

<%= link_to 'New Place', "/place/new" %>