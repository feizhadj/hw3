<h1>Edit Place</h1>

<%= form_for @place do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Back', place_path %>
